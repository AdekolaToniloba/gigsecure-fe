import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Verify Email',
  description: 'Verify your GigSecure email address.',
};

export default function VerifyEmailPage() {
  return <div className="sr-only">Verify email</div>;
}
